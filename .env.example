# Esta es una plantilla. Reemplace los valores a continuación con sus credenciales reales.

# JWT_SECRET: Clave secreta para firmar y verificar los tokens JWT. Use una cadena larga y aleatoria.
JWT_SECRET=your_jwt_secret

# DATABASE_URL: URL de conexión a la base de datos. Siga el formato específico para su DB (ejemplo para MySQL, PostgreSQL, etc.).
DATABASE_URL=your_database_connection_url
